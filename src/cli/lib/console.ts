import debug from 'debug'

export const info = debug('manifest-info')
export const verbose = debug('manifest-verbose')
export const silly = debug('manifest-silly')
export const warning = debug('manifest-warning')
export const error = debug('manifest-error')